# AiPulse — סדר בנייה

שישה שלבים. אחרי כל שלב: `npm run build` עובר, ואתה בודק בדפדפן.
אל תתחיל שלב לפני שהקודם עומד בקריטריונים שלו.

---

## שלב 0 — שלד

**מה לבנות**

- `npx create-next-app@latest` — TypeScript, Tailwind, App Router, בלי `src/`.
- `app/layout.tsx` עם `<html lang="he" dir="rtl">` ושלושת הגופנים דרך `next/font/google`.
- `app/globals.css` עם טוקני הפלטה מ-`SPEC.md` §7.
- העתקת הנכסים מ-`assets/` ל-`public/`.
- דף בית זמני שמציג את שם המותג ואת הסימן.

**קריטריון סיום** — העמוד עולה בעברית מימין לשמאל, בגופנים הנכונים, ומחליף
צבעים כשמשנים את מצב המערכת מבהיר לכהה.

---

## שלב 1 — שכבת התוכן

**מה לבנות**

- `lib/schema.ts` — הסכימה מ-`SPEC.md` §3.1, מילה במילה.
- `lib/constants.ts` — `FAMILIES` ו-`TOPICS`, מילה במילה.
- `content/items.json` — כבר קיים, 34 פריטים אמיתיים. אל תיגע בו.
- `lib/content.ts` — קריאה, ולידציה, מיון, `LAST_SCAN`, וגם `RECENT`,
  `MONTHS` ו-`byMonth` (`SPEC.md` §3.3). הארכיון לא נגזם — ראה §3.4.

**קריטריון סיום** — הבנייה עוברת. שינוי מכוון של פריט אחד ב-JSON (למשל מחיקת
`url`) מפיל את הבנייה עם הודעה ברורה. החזר אותו אחר כך.

---

## שלב 2 — הפיד

**מה לבנות**

- `components/feed/item-card.tsx` — כרטיס לפי הפירוט ב-`SPEC.md` §6.
- `components/ui/family-icon.tsx` — מיפוי משפחה לאייקון, inline.
- `app/feed/page.tsx` — קומפוננטת שרת שמעבירה את `RECENT` ל-`FeedList`.
- `app/archive/page.tsx` ו-`app/archive/[month]/page.tsx` — אינדקס חודשים
  וחודש בודד, עם `generateStaticParams` ו-`metadata` ייחודי לכל חודש.
- `components/feed/feed-list.tsx` — `"use client"`, כרגע רק מציג רשימה ממוינת.

**קריטריון סיום** — הפריטים מוצגים בסדר יורד, כל כותרת מקשרת ליעד הנכון,
העיגול מלא למקורות ראשוניים וחלול למשניים, והתצוגה תקינה ב-375 פיקסל.
`/archive` מונה את החודשים, ולחיצה על חודש מציגה את פריטיו.

**מספרים צפויים עם התוכן הנוכחי:** 34 פריטים בסך הכול, מהם 33 בחלון 30 הימים.
הפריט היחיד שנופל מחוץ לחלון הוא מ-2026-01-28. שלושה חודשים בארכיון:
2026-09, 2026-08, 2026-01. אם קיבלת מספרים אחרים — יש באג בחישוב החלון.

---

## שלב 3 — מצב המשתמש

**מה לבנות**

- `lib/user-state.ts` — הטיפוס, הממשק `StateStore`, מימוש `LocalStore`,
  הכול ב-`try/catch`.
- `lib/ranking.ts` — `scoreOf` ופונקציית המיון.
- חיווט שלוש הפעולות: הצמד, שמור, כבר קראתי.
- `components/feed/family-filter.tsx`, `topic-prefs.tsx`, `view-tabs.tsx`.
- תצוגת "נקראו" עם החזרה לפיד וניקוי הרשימה.

**קריטריון סיום**

- סימון "כבר קראתי" מוריד את הפריט; רענון הדף — הוא עדיין מוסתר.
- הצמדה מעלה את הפריט לקבוצה נפרדת בראש.
- כיבוי משפחת מקור מסתיר את כל פריטיה ומעדכן את המונה.
- סימון נושא כ"יותר" + מיון "לפי העדפות" משנה את הסדר בפועל.
- **בדיקת גלישה פרטית:** האתר נטען ומתפקד, גם אם המצב לא נשמר.
- אין הבהוב או קפיצה של הרשימה בטעינה.

---

## שלב 4 — דף הנחיתה ועמוד השיטה

**מה לבנות**

- `app/page.tsx` — חמשת החלקים מ-`SPEC.md` §6, כולם קומפוננטות שרת.
- `components/brand/hero-graphic.tsx` — הגרפיקה inline.
- `app/about/page.tsx` — טבלת המשפחות, ההצהרה על רשתות חברתיות, וגילוי נאות.
- פוטר בכל עמוד עם שורת הגילוי (`SPEC.md` §6).
- `components/ui/theme-toggle.tsx` — החלפה ידנית שכותבת `data-theme` על `<html>`,
  עם סקריפט קטן ב-`<head>` שמונע הבהוב בטעינה.

**קריטריון סיום** — Lighthouse 95+ בכל ארבע הקטגוריות בדסקטופ. המספרים בדף
הנחיתה מחושבים מהתוכן ולא מוקלדים. ניווט מקלדת מלא עם פוקוס נראה.

---

## שלב 5 — פריסה

**מה לבנות**

- דחיפה ל-GitHub, ייבוא ב-Vercel, דיפלוי.
- שם הפרויקט קובע את הכתובת: `<שם>.vercel.app`. נסה `aipulse`; אם תפוס,
  Vercel יציע חלופה. אין דומיין מותאם.
- בדיקה בטלפון אמיתי, לא רק ב-devtools.

**קריטריון סיום** — הכתובת הציבורית עולה, שני מצבי התצוגה עובדים, והסימון
נשמר בין ביקורים באותו דפדפן.

---

## שלב 6 — סנכרון בין מכשירים (אחרי שהאתר באוויר)

בונים אותו רק אחרי ששלב 5 יציב. `SPEC.md` §11 מפרט את המנגנון.

**מה לבנות**

- `syncId` אקראי שנוצר בכניסה ראשונה ונשמר מקומית. **בלי שום פנייה לרשת**
  כל עוד המשתמש לא ביקש סנכרון.
- `SyncedStore` שעוטף את `LocalStore` מאחורי אותו ממשק `StateStore`.
- `GET/PUT /api/state/[syncId]` מול Vercel KV, עם הגבלת קצב ותקרת גודל.
- ממשק: כפתור ״סנכרן מכשיר״ שמציג את הקוד, ושדה להקלדת קוד ממכשיר אחר.
- טקסט גלוי ליד הקוד: הוא המפתח היחיד, מי שמחזיק בו רואה ומשנה, אין סיסמה.

**קריטריון סיום** — סימון פריט במחשב מופיע בטלפון אחרי הקלדת הקוד. ניתוק
הרשת לא שובר את האתר — הוא ממשיך לעבוד מקומית.

---

## אחרי הפריסה

עדכן את המשימה המתוזמנת שכותבת את הסריקה: במקום לפרסם ארטיפקט, שתכתוב את
`content/items.json` ותדחוף לריפו. `SPEC.md` §9 מפרט. אל תעשה זאת לפני ששלב 5
עומד — עדיף שהתוכן יהיה קפוא בזמן הבנייה.

## החלטות שכבר התקבלו — אל תפתח אותן מחדש

| נושא | ההכרעה |
|---|---|
| שפה | עברית בלבד. אין תשתית רב-לשונית ואין צורך בה. |
| חשבונות | אין. לא בשלב 1 ולא אחר כך. |
| סנכרון | קוד אנונימי, שלב 6, אחרי שהאתר באוויר. |
| ארכיון | מצטבר. שום פריט לא נמחק. הפיד = 30 יום, הארכיון = הכול. |
| גילוי נאות | חובה, בפוטר ובעמוד השיטה. |
| ניוזלטר | לא בשלב 1. |
| דומיין | `<שם>.vercel.app`. אין דומיין מותאם. |
| תת-כותרת | ״סריקה יומית של AI בבריאות״. |
