# NOTES — מה שצריך לדעת ולא נמצא במסמכים האחרים

הקובץ הזה קיים כדי שאף החלטה לא תהיה תלויה בשיחה שנסגרה.

---

## הדבקה לפתיחת שיחה חדשה

העתק את הפסקה הזאת לתחילת כל שיחה חדשה שמלווה את הפרויקט:

> אני בונה את AiPulse — אתר עברי לסריקה יומית של AI בבריאות, ב-Next.js על Vercel.
> בתיקיית הפרויקט יש `CLAUDE.md`, `PRD.md`, `SPEC.md`, `BUILD_ORDER.md` ו-`NOTES.md`.
> קרא את כולם לפני שאתה עונה. אני עובד לפי `BUILD_ORDER.md`, שלב אחד בכל פעם.
> ההחלטות כבר התקבלו ומופיעות בטבלה בסוף `BUILD_ORDER.md` — אל תפתח אותן מחדש.

---

## היחס בין הסריקה היומית לאתר — שני דברים נפרדים כרגע

יש משימה מתוזמנת שרצה כל בוקר ב-07:12 שעון ישראל ומעדכנת **דשבורד ארטיפקט**,
לא את האתר. מזהה המשימה: `trig_019eeQFU9WjV2aPWVwFFCsWY`.

- `content/items.json` שבפרויקט הוא **צילום מצב מ-5 בספטמבר 2026** — 34 פריטים.
- הדשבורד ימשיך לצבור פריטים חדשים כל יום. האתר לא.
- שום דבר לא הולך לאיבוד: כשהאתר יהיה מוכן אפשר לייצא מהדשבורד `items.json` טרי.

**מה לעשות אחרי הפריסה** (`SPEC.md` §9): להחליף את הפרומפט של המשימה כך
שיכתוב ל-`content/items.json` בריפו וידחוף commit, במקום לפרסם ארטיפקט.
בעדכון הזה צריך גם לבטל שני כללים שקיימים בפרומפט הנוכחי:

1. **הגזימה ל-28 פריטים** — באתר הארכיון מצטבר ושום פריט לא נמחק.
2. **חסימת פריטים לפי היסטוריית קריאה** — בדשבורד ״כבר קראתי״ מנע החזרה של
   פריט. באתר רב-משתמשי אין ״נקרא״ גלובלי; הסריקה מדלגת רק על כתובות שכבר
   קיימות בקובץ.

---

## רישוי תמונות ווידאו

נבדק ואומת ב-5 בספטמבר 2026:

| מקור | מסחרי | ייחוס | הערות |
|---|---|---|---|
| Unsplash | מותר | לא נדרש | אסור לאסוף כדי לבנות שירות סטוק מתחרה |
| Pexels | מותר | לא נדרש | חל גם על וידאו. אסור למכור עותק לא-ערוך |

**זהירות ב-Unsplash:** תוצאות המסומנות **Unsplash+** הן מנוי בתשלום ואינן
מכוסות ברישיון החופשי.

**מאמרים בגישה פתוחה:** ב-npj Digital Medicine הרישיון נקבע **לכל מאמר בנפרד** —
או CC BY (מותר מסחרי ועריכה, בכפוף לקרדיט) או CC BY-NC-ND (לא מסחרי, בלי
נגזרות). חובה לבדוק את שורת הרישיון בתחתית כל מאמר. אותו כלל ב-medRxiv וב-arXiv.

**אסור:** תמונות מהבלוגים של Google Research ו-Microsoft Research מוגנות
בזכויות יוצרים.

---

## אזהרות על קבצי מדיה

**וידאו 4K הוא מלכודת ביצועים.** קובץ `uhd_4096_2160` טיפוסי מ-Pexels שוקל
עשרות מגה-בייט. כרקע בדף נחיתה הוא הורס את יעד ה-Lighthouse 95 שהוגדר ב-PRD.
אם רוצים וידאו: לדחוס ל-1080p ולכמה מגה-בייט, `muted autoplay loop playsinline`,
ועם `poster` שנטען ראשון.

**צילומי סטוק בקטגוריה הזאת הם קלישאה.** רובוטים לבנים ומוחות ממעגלים חשמליים
לא מוסיפים מידע לאתר שעוסק בראיות קליניות, והקורא המקצועי מזהה אותם כמילוי.
הנכסים ב-`assets/` הם מקוריים ונועדו בדיוק להימנע מזה.

---

## שתי מלכודות RTL שכבר עלו בפרויקט הזה

**1. `inset-inline-start` נפתר לפי ה-`direction` של האלמנט עצמו.** הוספת
`direction:ltr` לאלמנט ממוקם הופכת את המיקום שלו לצד השני. שם המותג AiPulse
לא צריך `direction:ltr` — הוא רצף LTR אחיד ממילא.

**2. `currentColor` לא חוצה גבול `<img>`.** קובץ SVG שנטען כתמונה הוא מסמך
מבודד שלא יורש `color`. בגלל זה יש `assets/icons/` (צבע עצמי, ל-`<img>`)
ו-`assets/icons-inline/` (currentColor, להטמעה ב-HTML).

**3. מחרוזת שמתחילה בתו ניטרלי** (`#`, `(`, `/`) בהקשר RTL נזרקת לצד הלא נכון.
קודי צבע וכתובות URL צריכים `direction:ltr` על אלמנט טקסט שאינו ממוקם.

---

## מקור הנכסים

`assets/` נוצר עבור הפרויקט הזה — סימן, פאביקון, גרפיקת פתיח ושמונה אייקוני
משפחות. אין עליהם שאלת רישוי. `assets/README.md` מפרט את הרעיון מאחורי הסימן
ואת אופן השימוש. הסימן גיאומטרי בלבד ולכן ניטרלי לשפה — הוא שרד שינוי שם
בלי עריכה.
